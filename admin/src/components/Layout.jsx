// Updated Layout.jsx (no changes needed, but included for completeness)
import React from "react";

const Layout = ({ children }) => {
  return <div className="min-h-screen bg-emerald-50">{children}</div>;
};

export default Layout;
